program: main_train.py
project: sweep_debugging
method: random
metric:
  goal: maximize
  name: Val accuracy
parameters:
  train_dir:
    value: "/mnt/disks/data/IXI_sedghi_subset_2upscaled/npy/train"
  val_dir:
    value: "/mnt/disks/data/IXI_sedghi_subset_2upscaled/npy/val"
  learning_rate:
    distribution: uniform
    min: 0.0003
    max: 0.05
  dropout:
    distribution: uniform
    min: 0.0
    max: 0.5
